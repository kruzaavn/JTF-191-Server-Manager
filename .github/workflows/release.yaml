name: Release

on:
  release:
    types:
      - created

jobs:
  build:
    name: windows release
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@master
      - name: update rust version
        run: rustup install stable
      - name: compile and release
        run: cd installer && cargo build --release --locked
